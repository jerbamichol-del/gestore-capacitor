package com.gestore.spese;

import com.getcapacitor.Plugin;
import com.getcapacitor.annotation.CapacitorPlugin;

@CapacitorPlugin(name = "NativePlugins")
public class NativePluginsPlugin extends Plugin {
    // This is a dummy plugin class required by Capacitor
    // The actual plugins (NotificationListener and SMSReader) are registered separately
}
