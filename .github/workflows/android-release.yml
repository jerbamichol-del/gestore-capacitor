name: Build Android APK

permissions:
  contents: write

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install --legacy-peer-deps
        env:
          SHARP_IGNORE_GLOBAL_LIBVIPS: 1
          npm_config_timeout: 600000

      - name: Build web assets
        run: npm run build

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Remove existing Android platform
        run: |
          if [ -d "android" ]; then
            echo "🗑️ Removing existing Android platform..."
            rm -rf android
          fi

      - name: Add Android platform
        run: |
          echo "📱 Creating Android platform..."
          npx cap add android
          echo "✅ Android platform created"

      # ✅ CRITICAL: Generate icons BEFORE sync
      - name: Generate app icons from resources/icon.png
        run: |
          echo "🎨 Generating app icons from resources/icon.png..."
          
          if [ ! -f "resources/icon.png" ]; then
            echo "❌ ERROR: resources/icon.png not found!"
            exit 1
          fi
          
          echo "📏 Icon size:"
          ls -lh resources/icon.png
          
          # Generate Android icons - this creates all mipmap folders
          npx @capacitor/assets generate --android
          
          echo "✅ Icons generated"
          echo "📂 Verifying generated icons:"
          ls -R android/app/src/main/res/mipmap-hdpi/ || echo "⚠️ mipmap-hdpi not found"
          ls -R android/app/src/main/res/mipmap-mdpi/ || echo "⚠️ mipmap-mdpi not found"
          ls -R android/app/src/main/res/mipmap-xhdpi/ || echo "⚠️ mipmap-xhdpi not found"
          
          # Verify at least one icon exists
          if [ -f "android/app/src/main/res/mipmap-hdpi/ic_launcher.png" ]; then
            echo "✅ ic_launcher.png verified in mipmap-hdpi"
            ls -lh android/app/src/main/res/mipmap-hdpi/ic_launcher.png
          else
            echo "❌ ERROR: Icons not generated!"
            exit 1
          fi

      - name: Sync Capacitor (preserves icons)
        run: |
          echo "🔄 Syncing Capacitor..."
          npx cap sync android
          echo "✅ Capacitor sync completed"
          
          echo "📂 Verifying icons still exist after sync:"
          ls -lh android/app/src/main/res/mipmap-hdpi/ic_launcher.png

      # ✅ CRITICAL: Setup debug keystore for consistent signing
      - name: Setup Debug Keystore
        run: |
          echo "🔑 Setting up debug keystore for consistent signing..."
          
          # Create debug keystore (fixed credentials for all builds)
          keytool -genkey -v \
            -keystore android/app/debug.keystore \
            -alias androiddebugkey \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass android \
            -keypass android \
            -dname "CN=Android Debug,O=Android,C=US"
          
          echo "✅ Debug keystore created"
          ls -lh android/app/debug.keystore
          
          # Copy signing configuration
          echo "📝 Applying signing configuration..."
          cat android-config/signing.gradle >> android/app/build.gradle
          
          echo "✅ Signing configuration applied"

      # ✅ AUTO VERSION BUMP
      - name: Bump Android versionCode
        run: |
          echo "🔢 Auto-incrementing versionCode..."
          chmod +x scripts/bump-version.sh
          ./scripts/bump-version.sh
          
          # Extract new version for release notes
          VERSION_CODE=$(grep -oP 'versionCode \K\d+' android/app/build.gradle)
          VERSION_NAME=$(grep -oP 'versionName "\K[^"]+' android/app/build.gradle)
          
          echo "NEW_VERSION_CODE=$VERSION_CODE" >> $GITHUB_ENV
          echo "NEW_VERSION_NAME=$VERSION_NAME" >> $GITHUB_ENV
          
          echo "📱 New version: $VERSION_NAME (Build $VERSION_CODE)"

      # ✅ Copy plugin files
      - name: Copy plugin Java files from android-config/plugins/
        run: |
          echo "📦 Copying plugin Java files from android-config/plugins/..."
          
          SOURCE_DIR="android-config/plugins"
          DEST_DIR="android/app/src/main/java/com/gestore/spese"
          
          mkdir -p "$DEST_DIR"
          
          cp "$SOURCE_DIR/NotificationListenerPlugin.java" "$DEST_DIR/"
          cp "$SOURCE_DIR/SMSReaderPlugin.java" "$DEST_DIR/"
          cp "$SOURCE_DIR/BankNotificationListenerService.java" "$DEST_DIR/"
          
          echo "✅ Java files copied to $DEST_DIR"
          ls -la "$DEST_DIR"

      - name: Copy AndroidManifest.xml
        run: |
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          
          echo "📝 Copying AndroidManifest.xml..."
          cp android-config/AndroidManifest.xml "$MANIFEST"
          
          if grep -q "BankNotificationListenerService" "$MANIFEST"; then
            echo "✅ Service declaration found"
          else
            echo "❌ ERROR: Service NOT found!"
            exit 1
          fi

      - name: Copy MainActivity with imports
        run: |
          MAIN_ACTIVITY="android/app/src/main/java/com/gestore/spese/MainActivity.java"
          
          echo "📝 Copying MainActivity.java..."
          cp android-config/MainActivity.java "$MAIN_ACTIVITY"
          
          if grep -q "registerPlugin(NotificationListenerPlugin.class)" "$MAIN_ACTIVITY"; then
            echo "✅ NotificationListenerPlugin registration found"
          else
            echo "❌ ERROR: NotificationListenerPlugin not registered!"
            exit 1
          fi
          
          if grep -q "registerPlugin(SMSReaderPlugin.class)" "$MAIN_ACTIVITY"; then
            echo "✅ SMSReaderPlugin registration found"
          else
            echo "❌ ERROR: SMSReaderPlugin not registered!"
            exit 1
          fi

      - name: Copy assets to Android
        run: |
          echo "📁 Copying assets..."
          npx cap copy android
          echo "✅ Assets copied"

      - name: Final verification before build
        run: |
          echo "🔍 Final verification..."
          
          JAVA_DIR="android/app/src/main/java/com/gestore/spese"
          
          # Check Java files
          for file in NotificationListenerPlugin.java SMSReaderPlugin.java BankNotificationListenerService.java MainActivity.java; do
            if [ -f "$JAVA_DIR/$file" ]; then
              echo "✅ $file exists"
            else
              echo "❌ ERROR: $file NOT found!"
              exit 1
            fi
          done
          
          # Check icons still exist
          if [ -f "android/app/src/main/res/mipmap-hdpi/ic_launcher.png" ]; then
            echo "✅ Icons verified before build"
          else
            echo "❌ ERROR: Icons missing before build!"
            exit 1
          fi
          
          # Check keystore exists
          if [ -f "android/app/debug.keystore" ]; then
            echo "✅ Debug keystore verified"
            ls -lh android/app/debug.keystore
          else
            echo "❌ ERROR: Debug keystore missing!"
            exit 1
          fi
          
          # Verify signing config in build.gradle
          if grep -q "signingConfigs" "android/app/build.gradle"; then
            echo "✅ Signing configuration found in build.gradle"
          else
            echo "❌ ERROR: Signing configuration NOT found!"
            exit 1
          fi

      - name: Build Debug APK with Signing
        run: |
          cd android
          chmod +x ./gradlew
          echo "🔨 Building signed debug APK..."
          ./gradlew clean assembleDebug --no-daemon --stacktrace
          
          echo "✅ Build completed"
          echo "📝 APK info:"
          ls -lh app/build/outputs/apk/debug/app-debug.apk

      - name: Rename APK with version
        run: |
          cp android/app/build/outputs/apk/debug/app-debug.apk gestore-spese-v${{ env.NEW_VERSION_NAME }}-build${{ env.NEW_VERSION_CODE }}.apk
          echo "✅ APK renamed to: gestore-spese-v${{ env.NEW_VERSION_NAME }}-build${{ env.NEW_VERSION_CODE }}.apk"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: gestore-spese-apk
          path: gestore-spese-v${{ env.NEW_VERSION_NAME }}-build${{ env.NEW_VERSION_CODE }}.apk

      - name: Get short commit SHA
        id: vars
        run: echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.NEW_VERSION_NAME }}-build${{ env.NEW_VERSION_CODE }}
          name: "Gestore Spese v${{ env.NEW_VERSION_NAME }} (Build ${{ env.NEW_VERSION_CODE }})"
          body: |
            🚀 **Versione ${{ env.NEW_VERSION_NAME }}** - Build #${{ env.NEW_VERSION_CODE }}
            
            📱 **SCARICA E INSTALLA:**
            1. Clicca su `gestore-spese-v${{ env.NEW_VERSION_NAME }}-build${{ env.NEW_VERSION_CODE }}.apk` qui sotto
            2. Apri il file scaricato sul tuo Android
            3. Se hai una versione precedente, questa la sostituirà automaticamente (stesso keystore)
            4. Installa
            
            ✅ **NOVITÀ DI QUESTA BUILD:**
            - Icona app personalizzata funzionante
            - Campanella notifiche si nasconde dopo abilitazione
            - APK firmato con keystore consistente (niente più conflitti!)
            - Sistema auto-update attivo
            
            ---
            
            ### 🎨 Componenti:
            - ✅ Icona app personalizzata (generata da resources/icon.png)
            - ✅ Sistema auto-update attivo
            - ✅ Plugin NotificationListener per transazioni
            - ✅ Plugin SMSReader per SMS bancari
            - ✅ versionCode: ${{ env.NEW_VERSION_CODE }}
            - ✅ Keystore debug consistente
            
            ---
            
            🔗 Commit: [${{ steps.vars.outputs.short_sha }}](https://github.com/${{ github.repository }}/commit/${{ github.sha }})
            🕐 Build: ${{ github.event.head_commit.timestamp }}
          files: gestore-spese-v${{ env.NEW_VERSION_NAME }}-build${{ env.NEW_VERSION_CODE }}.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
