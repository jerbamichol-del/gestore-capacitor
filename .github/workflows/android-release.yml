name: Build Android Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Serve per accedere a commit precedenti

      - name: 🔄 Ripristina CalculatorInputScreen dal commit funzionante
        run: |
          echo "🔄 Ripristino CalculatorInputScreen.tsx dal commit dc72f096..."
          git checkout dc72f096fac69f96cdc127f6c8309e19e4273dd9 -- components/CalculatorInputScreen.tsx
          echo "✅ File ripristinato"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: 🎨 Prepara icone per Android
        run: |
          echo "🎨 Prepara icone per Android"
          
          if [ -f "public/logo.png" ]; then
            echo "✅ Logo trovato in public/logo.png"
            mkdir -p assets
            cp public/logo.png assets/icon.png
            cp public/logo.png assets/icon-foreground.png
            echo "✅ Icone preparate:"
            ls -lh assets/icon*.png
          else
            echo "⚠️ Logo non trovato in public/logo.png"
            echo "Cerco in altre posizioni..."
            find . -name "logo.png" -o -name "icon.png" 2>/dev/null || echo "Nessun logo trovato"
          fi

      - name: 🔧 Fix safe-area calcolatrice
        run: |
          echo "🔧 Applicazione fix safe-area per CalculatorInputScreen"
          
          if [ -f "components/CalculatorInputScreen.tsx" ]; then
            # Modifica la riga specifica che contiene il tastierino
            sed -i 's/className="flex-shrink-0 flex flex-col"/className="flex-shrink-0 flex flex-col pb-[env(safe-area-inset-bottom)]"/g' components/CalculatorInputScreen.tsx
            
            echo "✅ Safe-area applicata"
            
            # Verifica modifica
            if grep -q "pb-\[env(safe-area-inset-bottom)\]" components/CalculatorInputScreen.tsx; then
              echo "✅ Modifica verificata con successo"
            else
              echo "⚠️ Modifica non trovata, provo pattern alternativo..."
              # Pattern alternativo se il primo non funziona
              sed -i 's/flex-shrink-0 flex flex-col" style/flex-shrink-0 flex flex-col pb-[env(safe-area-inset-bottom)]" style/g' components/CalculatorInputScreen.tsx
            fi
          else
            echo "⚠️ CalculatorInputScreen.tsx non trovato"
          fi

      - name: Build web assets
        run: npm run build

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Sync Capacitor
        run: npx cap sync android

      - name: 🎨 Generazione icone native
        run: |
          echo "🎨 Generazione icone native..."
          
          if [ -f "assets/icon.png" ]; then
            echo "✅ Icon preparato, eseguo capacitor-assets"
            npm install -g @capacitor/assets
            npx @capacitor/assets generate --assetPath ./assets --platform android || echo "⚠️ Generazione parzialmente fallita"
            
            echo "📊 Verifica icone generate:"
            find android/app/src/main/res -name "ic_launcher*.png" -o -name "ic_launcher*.xml" 2>/dev/null | head -20 || echo "Nessuna icona generata"
          else
            echo "⚠️ Nessun asset trovato in assets/icon.png, skip generazione"
          fi

      - name: Build APK
        run: |
          cd android
          chmod +x ./gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Sign APK
        run: |
          APK_PATH="android/app/build/outputs/apk/release/app-release-unsigned.apk"
          SIGNED_APK="gestore-spese.apk"
          
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > release.keystore
          
          $ANDROID_HOME/build-tools/34.0.0/zipalign -v -p 4 "$APK_PATH" aligned.apk
          
          $ANDROID_HOME/build-tools/34.0.0/apksigner sign --ks release.keystore \
            --ks-key-alias "${{ secrets.KEY_ALIAS }}" \
            --ks-pass pass:"${{ secrets.KEYSTORE_PASSWORD }}" \
            --key-pass pass:"${{ secrets.KEY_PASSWORD }}" \
            --out "$SIGNED_APK" \
            aligned.apk
          
          rm release.keystore aligned.apk

      - name: 📦 Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v1.0.${{ github.run_number }}
          release_name: Release v1.0.${{ github.run_number }}
          body: |
            🚀 **Release Automatica**
            
            ✅ Calcolatrice: safe-area fix applicato
            🎨 Icone: generazione nativa da public/logo.png
            🔒 Biometria: supporto nativo
            
            **Commit:** ${{ github.sha }}
            **Build:** #${{ github.run_number }}
          draft: false
          prerelease: false

      - name: Upload APK to Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./gestore-spese.apk
          asset_name: gestore-spese.apk
          asset_content_type: application/vnd.android.package-archive
