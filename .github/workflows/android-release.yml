name: Build Android APK

permissions:
  contents: write

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install --legacy-peer-deps
        env:
          SHARP_IGNORE_GLOBAL_LIBVIPS: 1
          npm_config_timeout: 600000

      - name: Build web assets
        run: npm run build

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Remove existing Android platform
        run: |
          if [ -d "android" ]; then
            echo "🗑️ Removing existing Android platform..."
            rm -rf android
          fi

      - name: Add Android platform
        run: |
          echo "📱 Creating Android platform..."
          npx cap add android
          echo "✅ Android platform created"

      - name: Verify Android structure
        run: |
          echo "🔍 Verifying Android project structure..."
          ls -la android/
          if [ ! -f "android/settings.gradle" ] && [ ! -f "android/settings.gradle.kts" ]; then
            echo "❌ ERROR: settings.gradle not found!"
            echo "Contents of android:" 
            find android/ -type f -name "*.gradle*" || true
            exit 1
          fi
          echo "✅ Android structure verified"

      # ✅ CRITICAL: Copy AndroidManifest.xml BEFORE cap sync!
      - name: Copy AndroidManifest.xml with permissions (BEFORE sync)
        run: |
          MANIFEST_DIR="android/app/src/main"
          mkdir -p "$MANIFEST_DIR"
          
          echo "📝 Copying AndroidManifest.xml template BEFORE sync..."
          cp android-config/AndroidManifest.xml "$MANIFEST_DIR/AndroidManifest.xml"
          echo "✅ AndroidManifest.xml copied"
          
          # Verify it was copied
          if grep -q "BankNotificationListenerService" "$MANIFEST_DIR/AndroidManifest.xml"; then
            echo "✅ Service declaration found in AndroidManifest.xml"
          else
            echo "❌ ERROR: Service declaration NOT found!"
            exit 1
          fi

      # ✅ NOW sync Capacitor (won't overwrite our AndroidManifest)
      - name: Sync Capacitor (includes native-plugins)
        run: |
          echo "🔄 Syncing Capacitor with native plugins..."
          npx cap sync android
          echo "✅ Capacitor sync completed"

      # ✅ Verify AndroidManifest wasn't overwritten
      - name: Verify AndroidManifest after sync
        run: |
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          echo "🔍 Verifying AndroidManifest.xml after sync..."
          
          if grep -q "BankNotificationListenerService" "$MANIFEST"; then
            echo "✅ Service declaration still present after sync"
          else
            echo "❌ ERROR: AndroidManifest was overwritten by cap sync!"
            echo "Copying it again..."
            cp android-config/AndroidManifest.xml "$MANIFEST"
          fi
          
          if grep -q "BIND_NOTIFICATION_LISTENER_SERVICE" "$MANIFEST"; then
            echo "✅ Notification listener permission found"
          else
            echo "⚠️ Warning: BIND_NOTIFICATION_LISTENER_SERVICE not found"
          fi

      - name: Generate native assets
        run: |
          echo "🎨 Generating native assets..."
          npx @capacitor/assets generate --android || echo "⚠️ Asset generation skipped (no assets found)"
          npx cap copy android
          echo "✅ Assets copied"

      # 🔍 Verify plugin was installed
      - name: Verify native plugins installation
        run: |
          echo "🔍 Verifying native-plugins installation..."
          if [ -d "node_modules/native-plugins" ]; then
            echo "✅ native-plugins found in node_modules"
            ls -la node_modules/native-plugins/android/src/main/java/com/gestore/spese/ || true
          else
            echo "❌ ERROR: native-plugins not found!"
            exit 1
          fi
          
          # Check if Capacitor detected the plugin
          echo "🔍 Checking Capacitor plugin detection..."
          if [ -d "android/native-plugins" ] || grep -r "native-plugins" android/capacitor.settings.gradle 2>/dev/null; then
            echo "✅ Plugin detected by Capacitor"
          else
            echo "⚠️ Warning: Plugin may not be properly detected"
            echo "Capacitor settings:"
            cat android/capacitor.settings.gradle 2>/dev/null || echo "capacitor.settings.gradle not found"
          fi
          
          # Check if plugin classes exist in android folder
          if find android -name "NotificationListener.java" 2>/dev/null | grep -q .; then
            echo "✅ NotificationListener.java found in android folder"
            find android -name "NotificationListener.java"
          else
            echo "⚠️ Warning: NotificationListener.java not found in android folder"
          fi

      # ✅ Build
      - name: Build Debug APK
        run: |
          cd android
          chmod +x ./gradlew
          ./gradlew clean assembleDebug --no-daemon --stacktrace

      - name: Rename APK
        run: |
          cp android/app/build/outputs/apk/debug/app-debug.apk gestore-spese.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: gestore-spese-apk
          path: gestore-spese.apk

      - name: Get short commit SHA
        id: vars
        run: echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: build-${{ steps.vars.outputs.short_sha }}
          name: "Build ${{ steps.vars.outputs.short_sha }}"
          body: |
            🤖 Build automatica da commit [${{ steps.vars.outputs.short_sha }}](https://github.com/${{ github.repository }}/commit/${{ github.sha }})
            
            📱 **Scarica l'APK:**
            - Clicca su `gestore-spese.apk` qui sotto
            - Oppure scansiona il QR code nell'app
            
            🕐 Build del: ${{ github.event.head_commit.timestamp }}
            
            ✅ **Questo build include:**
            - 📦 Plugin locale native-plugins (CORRETTO!)
            - ✅ NotificationListener con @CapacitorPlugin
            - ✅ SMSReader con @CapacitorPlugin
            - ✅ BankNotificationListenerService
            - ✅ AndroidManifest copiato PRIMA di cap sync
            - ✅ Permessi Android corretti
            - ✅ Package: com.gestore.spese
          files: gestore-spese.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
