name: Build Android APK

permissions:
  contents: write

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          npm install --legacy-peer-deps
          npm install --save-dev cordova-res sharp
        env:
          SHARP_IGNORE_GLOBAL_LIBVIPS: 1
          npm_config_timeout: 600000

      - name: Build web assets
        run: npm run build

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Remove existing Android platform
        run: |
          if [ -d "android" ]; then
            echo "🗑️ Removing existing Android platform..."
            rm -rf android
          fi

      - name: Add Android platform
        run: |
          echo "📱 Creating Android platform..."
          npx cap add android
          echo "✅ Android platform created"

      # ✅ CRITICAL: Resize splash to meet cordova-res requirements
      - name: Prepare resources for cordova-res
        run: |
          echo "🎨 Preparing resources for cordova-res..."
          
          # Install ImageMagick (already available on ubuntu-latest)
          sudo apt-get update -qq
          sudo apt-get install -y -qq imagemagick
          
          # Create resources/android directory
          mkdir -p resources/android
          
          # Resize icon to 1920x1920 for splash (cordova-res requirement)
          # Using ImageMagick to resize and center on white background
          convert resources/icon.png \
            -resize 1920x1920 \
            -background white \
            -gravity center \
            -extent 1920x1920 \
            resources/splash.png
          
          echo "✅ Splash created at 1920x1920"
          ls -lh resources/splash.png
          
          # Copy for adaptive icons
          cp resources/icon.png resources/android/icon-foreground.png
          cp resources/icon.png resources/android/icon-background.png
          
          echo "✅ Resources prepared"
          ls -lh resources/
          ls -lh resources/android/

      # ✅ Generate icons and splash with cordova-res
      - name: Generate app icons and splash
        run: |
          echo "🎨 Generating icons and splash with cordova-res..."
          
          # cordova-res with both icon and splash
          npx cordova-res android --skip-config --copy
          
          echo "✅ Icons and splash generated"
          
          # Verify generated icons
          echo "🔍 Checking generated icons:"
          if [ -f "android/app/src/main/res/mipmap-hdpi/ic_launcher.png" ]; then
            echo "✅ ic_launcher.png exists in mipmap-hdpi"
            ls -lh android/app/src/main/res/mipmap-hdpi/ic_launcher.png
          else
            echo "❌ ic_launcher.png NOT found!"
            exit 1
          fi
          
          # List all generated resources
          echo "📂 All mipmap directories:"
          find android/app/src/main/res -type d -name "mipmap-*" | sort
          
          echo "📂 All drawable directories:"
          find android/app/src/main/res -type d -name "drawable*" | sort

      - name: Sync Capacitor
        run: |
          echo "🔄 Syncing Capacitor..."
          npx cap sync android
          echo "✅ Capacitor sync completed"
          
          echo "🔍 Verifying icons after sync:"
          if [ -f "android/app/src/main/res/mipmap-hdpi/ic_launcher.png" ]; then
            echo "✅ Icons preserved after sync"
          else
            echo "❌ Icons LOST after sync!"
            exit 1
          fi

      # ✅✅✅ CRITICAL FIX: Use persistent keystore from repo instead of generating
      - name: Setup Persistent Debug Keystore
        run: |
          echo "🔑 Setting up PERSISTENT debug keystore..."
          
          # Check if keystore exists in repo
          if [ -f "android-config/debug.keystore" ]; then
            echo "✅ Found persistent keystore in repo"
            cp android-config/debug.keystore android/app/debug.keystore
            echo "✅ Keystore copied from repo"
          else
            echo "⚠️ WARNING: Persistent keystore NOT found in repo!"
            echo "⚠️ Generating a NEW keystore (this will cause package conflict!)"
            echo "⚠️ To fix: Generate keystore locally and commit to android-config/debug.keystore"
            
            # Generate as fallback (will cause conflicts)
            keytool -genkeypair -v \
              -storetype PKCS12 \
              -keystore android/app/debug.keystore \
              -alias androiddebugkey \
              -keyalg RSA \
              -keysize 2048 \
              -validity 10000 \
              -storepass android \
              -keypass android \
              -dname "CN=Android Debug,OU=Android,O=Android,L=Mountain View,ST=California,C=US"
          fi
          
          echo "✅ Debug keystore ready"
          ls -lh android/app/debug.keystore
          
          # Apply signing configuration
          echo "📍d Applying signing configuration..."
          cat android-config/signing.gradle >> android/app/build.gradle
          
          # Verify
          if grep -q "signingConfigs" "android/app/build.gradle"; then
            echo "✅ Signing config applied"
          else
            echo "❌ Signing config NOT applied!"
            exit 1
          fi

      - name: Bump Android versionCode
        run: |
          echo "🔢 Auto-incrementing versionCode..."
          chmod +x scripts/bump-version.sh
          ./scripts/bump-version.sh
          
          VERSION_CODE=$(grep -oP 'versionCode \K\d+' android/app/build.gradle)
          VERSION_NAME=$(grep -oP 'versionName "\K[^"]+' android/app/build.gradle)
          
          echo "NEW_VERSION_CODE=$VERSION_CODE" >> $GITHUB_ENV
          echo "NEW_VERSION_NAME=$VERSION_NAME" >> $GITHUB_ENV
          
          echo "📱 Version: $VERSION_NAME (Build $VERSION_CODE)"

      - name: Copy plugin files
        run: |
          echo "📦 Copying plugin files..."
          
          SOURCE_DIR="android-config/plugins"
          DEST_DIR="android/app/src/main/java/com/gestore/spese"
          
          mkdir -p "$DEST_DIR"
          
          cp "$SOURCE_DIR/NotificationListenerPlugin.java" "$DEST_DIR/"
          cp "$SOURCE_DIR/SMSReaderPlugin.java" "$DEST_DIR/"
          cp "$SOURCE_DIR/BankNotificationListenerService.java" "$DEST_DIR/"
          
          echo "✅ Plugin files copied"

      - name: Copy AndroidManifest.xml
        run: |
          echo "📝 Copying AndroidManifest.xml..."
          cp android-config/AndroidManifest.xml android/app/src/main/AndroidManifest.xml
          echo "✅ AndroidManifest copied"

      - name: Copy MainActivity
        run: |
          echo "📝 Copying MainActivity.java..."
          cp android-config/MainActivity.java android/app/src/main/java/com/gestore/spese/MainActivity.java
          echo "✅ MainActivity copied"

      - name: Copy assets
        run: |
          echo "📁 Copying web assets..."
          npx cap copy android
          echo "✅ Assets copied"

      - name: Final pre-build verification
        run: |
          echo "🔍 Final verification..."
          
          # Verify Java files
          JAVA_DIR="android/app/src/main/java/com/gestore/spese"
          for file in NotificationListenerPlugin.java SMSReaderPlugin.java BankNotificationListenerService.java MainActivity.java; do
            if [ -f "$JAVA_DIR/$file" ]; then
              echo "✅ $file"
            else
              echo "❌ $file MISSING!"
              exit 1
            fi
          done
          
          # Verify icon exists
          if [ -f "android/app/src/main/res/mipmap-hdpi/ic_launcher.png" ]; then
            echo "✅ Icon verified"
          else
            echo "❌ Icon MISSING!"
            exit 1
          fi
          
          # Verify keystore
          if [ -f "android/app/debug.keystore" ]; then
            echo "✅ Keystore verified"
          else
            echo "❌ Keystore MISSING!"
            exit 1
          fi
          
          echo "✅ All verifications passed"

      - name: Build APK
        run: |
          cd android
          chmod +x ./gradlew
          echo "🔨 Building signed APK..."
          ./gradlew assembleDebug --no-daemon --stacktrace
          echo "✅ Build completed"

      - name: Rename APK
        run: |
          cp android/app/build/outputs/apk/debug/app-debug.apk gestore-spese.apk
          echo "✅ APK ready: gestore-spese.apk"
          ls -lh gestore-spese.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: gestore-spese-apk
          path: gestore-spese.apk

      - name: Get commit info
        id: vars
        run: echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.NEW_VERSION_NAME }}-build${{ env.NEW_VERSION_CODE }}
          name: "v${{ env.NEW_VERSION_NAME }} Build ${{ env.NEW_VERSION_CODE }}"
          body: |
            🚀 **VERSIONE ${{ env.NEW_VERSION_NAME }}** - Build ${{ env.NEW_VERSION_CODE }}
            
            ---
            
            ## 📱 INSTALLAZIONE
            
            ### ✅ INSTALLAZIONE / AGGIORNAMENTO:
            
            1. Scarica `gestore-spese.apk`
            2. Apri il file sul tuo Android
            3. Se è la prima volta: abilita "Installa da sorgenti sconosciute"
            4. Se è un aggiornamento: Android chiederà "Vuoi aggiornare?"
            5. Installa/Aggiorna
            6. ✅ FATTO - Dati preservati!
            
            ### ⚠️ IMPORTANTE:
            
            Tutte le build usano lo STESSO keystore persistente. Gli aggiornamenti funzionano senza disinstallare!
            
            ---
            
            ## ✅ FIX IN QUESTA BUILD:
            
            1. **🔑 Keystore Persistente**: Stesso keystore per tutte le build - NO PIÙ CONFLITTI!
            2. **🚫 Schermata Bianca**: Fix definitivo con delay 300ms + error handling
            3. **🔔 Sistema Auto-Update**: Parsing corretto dei tag v1.0-build2
            4. **🎨 Icona App**: Generata con cordova-res
            5. **🔔 Campanella Notifiche**: Si nasconde dopo abilitazione
            6. **📝 Modal Notifiche**: Z-index corretto
            
            ---
            
            🔗 Commit: [${{ steps.vars.outputs.short_sha }}](https://github.com/${{ github.repository }}/commit/${{ github.sha }})
            
            📋 versionCode: ${{ env.NEW_VERSION_CODE }}
            📱 versionName: ${{ env.NEW_VERSION_NAME }}
          files: gestore-spese.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
